#include<stdlib.h>
#include<stdio.h>

int main () {
    
    int age = 40;
    double score = 3.6;
    char grade = 'A'; //one single character
    char grade2[] = "A++"; //more than one character

    /* printf function */
    //printf("Hello\"World\""); rendering coutes
    //printf("My favorite %s is: %d or %f", "number", 500); rendering strings, integer, float 
    
    return 0;
}