#include<stdlib.h>
#include<stdio.h>

int main() {

    int index = 0;

    while (index <= 100)
    {
        printf("%d\n", index);
        index = index + 1; // or index++;
    }
    
    
    return 0;
}