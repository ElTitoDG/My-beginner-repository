#include <stdio.h>

int main()
{
    int base, altura;

    base = 3;
    altura = 10;

    printf("Base: %d Altura: %d\n", base, altura);

    int area = base * altura;
    printf("Area: %d\n", area);
}